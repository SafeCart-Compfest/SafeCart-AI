# Data boundary

Only manifests, schemas, and small synthetic samples belong in Git. Raw, interim, and
processed datasets are ignored. Each source manifest must record its owner, permission or
license status, snapshot date, fields used, preprocessing, and SHA-256 hashes.
